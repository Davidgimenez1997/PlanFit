package com.utad.david.planfit.Utils;

import com.utad.david.planfit.Model.Sport.DefaultSport;

import java.util.ArrayList;
import java.util.List;

public class UtilsSports {

    /**
     * Slimming, Toning and Gain Volumen to Default
     */

    /*
    public static DefaultSport convertSportSlimmingToDefault(SportSlimming sportSlimming) {
        DefaultSport defaultSport = new DefaultSport();
        defaultSport.setName(sportSlimming.getName());
        defaultSport.setDescription(sportSlimming.getDescription());
        defaultSport.setPhoto(sportSlimming.getPhoto());
        defaultSport.setVideo(sportSlimming.getVideo());
        defaultSport.setType("adelgazar");
        return defaultSport;
    }

    public static List<DefaultSport> convertListSportSlimmingToDefaultList(List<SportSlimming> sportSlimming) {
        List<DefaultSport> defaultSportList = new ArrayList<>();
        for (SportSlimming item: sportSlimming) {
            DefaultSport defaultSport = new DefaultSport();
            defaultSport.setName(item.getName());
            defaultSport.setDescription(item.getDescription());
            defaultSport.setPhoto(item.getPhoto());
            defaultSport.setVideo(item.getVideo());
            defaultSport.setType("adelgazar");
            defaultSportList.add(defaultSport);
        }
        return defaultSportList;
    }

    public static DefaultSport convertSportToningToDefault(SportToning sportToning) {
        DefaultSport defaultSport = new DefaultSport();
        defaultSport.setName(sportToning.getName());
        defaultSport.setDescription(sportToning.getDescription());
        defaultSport.setPhoto(sportToning.getPhoto());
        defaultSport.setVideo(sportToning.getVideo());
        defaultSport.setType("tonificar");
        return defaultSport;
    }

    public static List<DefaultSport> convertListSportToningToDefaultList(List<SportToning> sportToning) {
        List<DefaultSport> defaultSportList = new ArrayList<>();
        for (SportToning item: sportToning) {
            DefaultSport defaultSport = new DefaultSport();
            defaultSport.setName(item.getName());
            defaultSport.setDescription(item.getDescription());
            defaultSport.setPhoto(item.getPhoto());
            defaultSport.setVideo(item.getVideo());
            defaultSport.setType("tonificar");
            defaultSportList.add(defaultSport);
        }
        return defaultSportList;
    }

    public static DefaultSport convertSportGainVolumenToDefault(SportGainVolume sportGainVolume) {
        DefaultSport defaultSport = new DefaultSport();
        defaultSport.setName(sportGainVolume.getName());
        defaultSport.setDescription(sportGainVolume.getDescription());
        defaultSport.setPhoto(sportGainVolume.getPhoto());
        defaultSport.setVideo(sportGainVolume.getVideo());
        defaultSport.setType("ganarVolumen");
        return defaultSport;
    }

    public static List<DefaultSport> convertListSportsGainVolumenToDefaultList(List<SportGainVolume> sportGainVolume) {
        List<DefaultSport> defaultSportList = new ArrayList<>();
        for (SportGainVolume item: sportGainVolume) {
            DefaultSport defaultSport = new DefaultSport();
            defaultSport.setName(item.getName());
            defaultSport.setDescription(item.getDescription());
            defaultSport.setPhoto(item.getPhoto());
            defaultSport.setVideo(item.getVideo());
            defaultSport.setType("ganarVolumen");
            defaultSportList.add(defaultSport);
        }
        return defaultSportList;
    }

     */

    /**
     * Default to Slimming, Toning and Gain Volumen
     */

    /*
    public static SportSlimming convertDefaultToSportSlimming(DefaultSport defaultSport) {
        SportSlimming sportSlimming = new SportSlimming();
        sportSlimming.setName(defaultSport.getName());
        sportSlimming.setDescription(defaultSport.getDescription());
        sportSlimming.setPhoto(defaultSport.getPhoto());
        sportSlimming.setVideo(defaultSport.getVideo());
        sportSlimming.setType(defaultSport.getVideo());
        return sportSlimming;
    }

    public static SportToning convertDefaultToSportToning(DefaultSport defaultSport) {
        SportToning sportToning = new SportToning();
        sportToning.setName(defaultSport.getName());
        sportToning.setDescription(defaultSport.getDescription());
        sportToning.setPhoto(defaultSport.getPhoto());
        sportToning.setVideo(defaultSport.getVideo());
        sportToning.setType(defaultSport.getVideo());
        return sportToning;
    }

    public static SportGainVolume convertDefaultToSportGainVolumen(DefaultSport defaultSport) {
        SportGainVolume sportGainVolume = new SportGainVolume();
        sportGainVolume.setName(defaultSport.getName());
        sportGainVolume.setDescription(defaultSport.getDescription());
        sportGainVolume.setPhoto(defaultSport.getPhoto());
        sportGainVolume.setVideo(defaultSport.getVideo());
        sportGainVolume.setType(defaultSport.getVideo());
        return sportGainVolume;
    }
    */
}
